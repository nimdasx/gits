ubuntu desktop disable nvidia mergo crash langsung restart pas switching ke nvidia

google chrome 
Settings > System > uncheck "Use graphics acceleration when available"

/etc/default/grub
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash nouveau.modeset=0 acpi_osi="

sudo update-grub

/etc/modprobe.d/blacklist-nouveau.conf
blacklist nouveau
options nouveau modeset=0

sudo update-initramfs -u